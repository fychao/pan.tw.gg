#include <stdio.h>
int main(void)
{
    
    char name;
    int mode;
    pirntf("Hello!Welcome to ROT-13 EN/DEcode machine!!What's your name?\n");
    scanf("%s", name);
    printf("Hi!%s!\nWhat mode do you want?(1 for encode,2 for decode)\n", name);
    scanf("%d",mode);
    if (mode == 1)
    { 
        char words;
        pirntf("Input words:\n");
        scanf("%s", words);
        idx = 0;
        while(word[idx] !='\0')
        {
            printf("%c", name[idx++];)
            while("%d",words >=67 && <=91)
            {

            }
            while("%d",words >=97 && <=123)
            {

            }

        }
    } else {

        char code;
        pirntf("Input codes:\n");
        scanf("%s", code);
        idx = 0;
        while(code[idx] !='\0')
        {
            while("%d",code >=67 && <=91)
            {

            }
            while("%d",code >=97 && <=123)
            {

            }


    }

    return 0;
}
